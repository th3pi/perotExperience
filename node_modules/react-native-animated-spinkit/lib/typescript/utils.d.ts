import { EasingFunction, Animated } from 'react-native';
export declare function createKeyframeEasingFunction(keyframes: number[], easing: EasingFunction): (t: number) => number;
export interface AnimationConfig {
    duration: number;
    value: Animated.Value;
    keyframes?: number[];
    toValue?: number;
    easing?: EasingFunction;
    infinite?: boolean;
    delay?: number;
}
export declare function anim({ duration, value, keyframes, easing, toValue, infinite, delay, }: AnimationConfig): Animated.CompositeAnimation;
export declare function stagger(time: number, amount: number, animationConfig: AnimationConfig): {
    animation: Animated.CompositeAnimation;
    values: (Animated.Value | Animated.AnimatedInterpolation)[];
};
